import React from "react";
import { Result, Button } from "antd";
import { Link } from "react-router-dom";

const Confirmacion = () => (
  <Result status="success" title={"Se ha creado la persona exitosamente!"} />
);

export default Confirmacion;
